services:
  db:
    image: postgres:latest
    container_name: giesbert-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    expose:
      - "5432"
    volumes:
      - giesbert-db:/var/lib/postgresql

volumes:
  giesbert-db:
