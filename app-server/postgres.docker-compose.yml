services:
  db:
    image: postgres:18
    container_name: giesbert-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - giesbert-db:/var/lib/postgresql/data

volumes:
  giesbert-db:
